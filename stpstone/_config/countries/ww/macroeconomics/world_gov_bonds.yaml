### CONFIGURATION TOOLS ###

credentials:
  host: https://www.worldgovernmentbonds.com/
  headers:
    'accept': '*/*'
    'accept-language': 'en-US,en;q=0.9,pt;q=0.8,es;q=0.7'
    'content-type': 'application/json; charset=UTF-8'
    'origin': 'https://www.worldgovernmentbonds.com'
    'priority': 'u=1, i'
    'referer': 'https://www.worldgovernmentbonds.com/'
    'sec-ch-ua': '"Chromium";v="136", "Google Chrome";v="136", "Not.A/Brand";v="99"'
    'sec-ch-ua-mobile': '?0'
    'sec-ch-ua-platform': '"Linux"'
    'sec-fetch-dest': 'empty'
    'sec-fetch-mode': 'cors'
    'sec-fetch-site': 'same-origin'
    'user-agent': 'Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/136.0.0.0 Safari/537.36'
    'Cookie': '_ga=GA1.1.986309816.1747186644; _ga_K8P6HJT6M9=GS2.1.s1747212162$o3$g1$t1747212175$j0$l0$h0'
  payload: "{\"DUMMY\":null}"
  token:
    host:
    app:
    headers:
    get:
      req_method: GET
      bl_verify: False
      timeout: (12.0, 12.0)
    keys:
      token: token
  web_driver:

metadata:


### RESOURCES TO BE SCRAPED ###

sovereign_spreads:
  web_driver:
    xpath_el_wait_until_loaded:
  req_method: POST
  # if host is not defined within credentials, use the source one
  host:
  headers:
  payload: "{\"DUMMY\":null}"
  # list of slugs (complementary descriptive text to access a web pages)
  slugs:
  # use this argument only if {{chunk_slugs}} is passed to app
  int_chunk_slugs:
  # iteratively_get_data placeholders:
  #   - i (int): https://example.com/{{i}}
  #   - slug (str): https://example.com/{{slug}} - slugs in list format
  #   - chunk_slugs (List[str]): https://example.com/{{chunk_slugs}}
  # non-iteratively_get_data placeholders:
  #   - {{replacer}}: https://example.com/{{replacer}}
  #     note: the replacer can be any variable referenced within concrete product class
  # expected comments to app, within url:
  #   - feat=read_tables
  #   - int_pgs_join={{number}}
  #   - file_extension=.{{file_extension}}
  #   - bl_separator_consistency_check == False, as default
  #   - start with a commentary hash and separate params with &
  # for using source and bl_debug dummy variables, please pass #source=dummy_1&bl_debug=True within
  #   the app; example: application/name/#source=dummy_1&bl_debug=True
  app: wp-json/home/v1/main#format=.html_regex
  bl_verify: False
  timeout: (12.0, 12.0)
  cookies:
  cols_from_case: default
  cols_to_case: upper_constant
  list_cols_drop_dupl:
  str_fmt_dt: YYYY-MM-DD
  type_error_action: raise
  strategy_keep_when_dupl: first
  list_ignorable_exceptions:
  schema: raw
  table_name: ww_gov_bonds_sovereign_spreads
  bl_insert_or_ignore: False
  bl_schema: True
  ignored_file_extensions:
    - tmp
    - log
    - bak
  fillna_strt:
    COL_1: bfill
    COL_2: ffill
  dtypes:
    EVENT: str
    MATCH_PATTERN: str
    PATTERN_REGEX: str
    REGEX_GROUP_0: str
    REGEX_GROUP_1: str
  regex_patterns:
    country_name:
      condition_1: (?i)class=\\"linkToCountry\\">\\n\\t\\t\\t\\t\s*([^*]+?)[\s+\(|\t]
    rating:
      condition_1: (?i)sorttable_customkey=\\"\d+[^*]+?linkToCountry\\"[^*]+?\s+\\t\\t\\t\\t\s+([^^*]+?)\s+
    10y_bond_yield:
      condition_1: (?i)class=\\"linkT10Y\\">([^*]+?)<\\.a>
    bank_rate:
      condition_1: (?i)\.\d+\\">\\n\s*<div\s+class=\\"[^*]+?\\n\s+([^*]+?)\\n\s+<\\.div>
    spread_vs_bund:
      condition_1: (?i)\\"linkToGerSpread\\">([^*]+?)<\\.a>
    spread_vs_tnote:
      condition_1: (?i)\\"linkToUsaSpread\\">([^*]+?)<\\.a>
    spread_vs_bank_rate:
      condition_1: (?i)\\"linkToUsaSpread\\"[^*]+?sorttable_customkey[^*]+?\s+([^*]+?)\\n
### MAIN SETTINGS ###

# requests
requests:
  successful_conn: 200
  unsuccesful_conn: 404

# audit log cols for storage banks
audit_log_cols:
  url: URL
  last_update: LAST_UPDATE
  log_timestamp: LOG_TIMESTAMP
  user: USER
  host: HOST
  error_msg: ERROR_MSG
  action_type: ACTION_TYPE
  action_timestamp: ACTION_TIMESTAMP